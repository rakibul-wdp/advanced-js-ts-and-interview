/**

How do you find the index in array map method?

1. On first parameter
2. On second parameter
3. Not available in map
4. Just need to destructure to get

Ans: 2
 
*/

const numbers = [2, 3, 5, 7];

numbers.map((item, index) => console.log(item, "index", index));
